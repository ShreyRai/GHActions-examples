name: "Handles issue"
on: 
    issues:
        types: opened
jobs:
    output-info:
        runs-on: ubuntu-latest
        steps:
            - name: Output of the workflow
              run: echo "${{ toJSON(github.event) }}"
