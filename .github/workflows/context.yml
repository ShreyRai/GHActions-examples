name: Concurrency check
on:
    push:
        branches:
            [concurrency]
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: Push count
              run: echo "This is push no. 2"
            - name: Go to sleep
              run: sleep 300
